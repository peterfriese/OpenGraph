// swift-tools-version:5.3
import PackageDescription

let package = Package(
    name: "OpenGraph",
    products: [
      .library(
        name: "OpenGraph",
        targets: ["OpenGraph"])
    ],
  targets: [
    .target(
      name: "OpenGraph",
      dependencies: [])
  ]
)
