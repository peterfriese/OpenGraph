// swift-tools-version:4.0
import PackageDescription

let package = Package(
    name: "OpenGraph",
    products: [
      .library(
        name: "OpenGraph",
        targets: ["OpenGraph"])
    ],
  targets: [
    .target(
      name: "OpenGraph",
      dependencies: [])
  ]
)
