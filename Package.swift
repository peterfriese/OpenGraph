// swift-tools-version:5.4.0
import PackageDescription

let package = Package(
    name: "OpenGraph",
    products: [
      .library(
        name: "OpenGraph",
        targets: ["OpenGraph"])
    ],
  targets: [
    .target(
      name: "OpenGraph",
      dependencies: [])
  ]
)
